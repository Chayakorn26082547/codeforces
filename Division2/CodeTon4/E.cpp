// Monsters
#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0),cin.tie(0);
    int q;
    cin >> q;
    while(q--)
    {
        int n,m;
        cin >> n >> m;
        for(int i=1;i<=n;i++)
        {
            cin >> a[i];
            if(a[i]==0)
                a[i]
        }
    }
    return 0;
}